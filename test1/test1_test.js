'use strict';

describe('myApp.amortization module', function() {

  beforeEach(module('myApp.amortization'));

  describe('amortization controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var amortizationCtrl = $controller('AmortizationCtrl');
      expect(amortizationCtrl).toBeDefined();
    }));

  });
});